import React from 'react';

export default class Rooms extends React.PureComponent { // eslint-disable-line react/prefer-stateless-function
  render() {
    return (
      <div>Test Rooms</div>
    );
  }
}
